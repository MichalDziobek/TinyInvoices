@model IEnumerable<TinyInvoices.Models.DatabaseModel.Bill>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Charge.Cost.Name)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Charge.Invoice.UserGroup)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Charge.Invoice.CalculationDate)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Charge.Cost.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Charge.Invoice.UserGroup.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Charge.Invoice.CalculationDate)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.BillId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BillId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BillId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
